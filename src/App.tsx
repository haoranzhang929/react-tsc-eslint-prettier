const App = () => {
  return (
    <div style={{ display: "flex", height: "100vh", justifyContent: "center" }}>
      <h1 style={{ height: "fit-content", alignSelf: "center", width: "calc(100vw - 50%)", textAlign: "center" }}>
        React typescript template with eslint + prettier + tsc configuration
      </h1>
    </div>
  );
};

export default App;
